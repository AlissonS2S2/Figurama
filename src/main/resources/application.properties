# Configuração MySQL (para usar com Workbench/Docker)
spring.datasource.url=jdbc:mysql://localhost:3306/figurama_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=figurama
spring.datasource.password=figurama123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Configuração H2 em memória para desenvolvimento (comentado)
#spring.datasource.url=jdbc:h2:mem:figurama_db
#spring.datasource.username=sa
#spring.datasource.password=
#spring.datasource.driver-class-name=org.h2.Driver

# Configuração do H2 Dialect (comentado)
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Use 'validate' para não recriar tabelas que já existem
spring.jpa.hibernate.ddl-auto=validate 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Habilitar console H2 (comentado - não usado com MySQL)
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

# === CONFIGURAÇÃO DE PORTA ===
# Altere a porta aqui (padrão: 8080)
server.port=8080